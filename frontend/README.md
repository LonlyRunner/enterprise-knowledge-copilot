# 星云科技有限公司知识库联调台

这是一个零构建依赖的静态联调页面，由 FastAPI 通过 `/ui/` 提供。

启动后打开：

```text
http://localhost:8000/ui/
```

页面支持：

- 后端健康检查和 API 地址切换
- 知识库创建、列表和切换
- `.txt`、`.md`、`.docx`、`.pdf` 文档上传
- 会话创建和切换
- `/api/v1/rag/chat` RAG 问答
- 最近一次请求的原始 JSON 调试信息

页面默认使用 `/api/v1`，因此和后端同源部署时不需要额外配置 CORS。
